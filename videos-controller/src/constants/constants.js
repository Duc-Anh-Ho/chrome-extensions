const speed = 100;
const VIDEO = {
    speed
}

const NUM = /^\d+(\.\d{0,2})?$/; // 0.00
const CHR = /[^\d.]/g;
const REGEX = {
    NUM
    , CHR
}

export {
    VIDEO
    , REGEX
}

export default {
    VIDEO
    , REGEX
}